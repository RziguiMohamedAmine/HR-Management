export class Notification {

  idNotif!: number; // Corresponds to Long in Java
  adminId!: number; // Admin ID associated with the notification
  message!: string; // Notification message
  timestamp!: string;
}
